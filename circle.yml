machine:
  php:
    version: 5.5.11
    
## Customize dependencies
dependencies:
  pre:
    - pear channel-discover pear.phing.info
    - pear install [--alldeps] phing/phing
    
deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER
